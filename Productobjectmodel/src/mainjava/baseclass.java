package mainjava;

import org.openqa.selenium.WebDriver;

public abstract class baseclass {   //this class not create a object 
	public static WebDriver driver;   //abstrct is not create a object
     protected Loginpage loginpage;
     protected dashboard dashboardpage;
}
